# ── Python environments ─────────────────────────────────────
.venv/
venv/
env/
ENV/

# ── Python runtime / build artifacts ────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# ── Secrets / env ───────────────────────────────────────────
.env
.env.local

# ── macOS / IDE ─────────────────────────────────────────────
.DS_Store
.vscode/
.idea/

# ── Streamlit local ─────────────────────────────────────────
.streamlit/secrets.toml

# ── Model / data outputs (keep local only) ─────────────────
.benchmarks/
.models/*.pkl
.models/*.joblib
.~lock.*#

# Local-only prediction store (Supabase is the production source of truth).
# These can carry user-specific data + scratch SQLite test artifacts.
.predictions/
retrain.log
*.log
retrain_status_*.md

# Excel/Numbers lock files + backups
~$*.xlsx
*.xlsx.bak
DB password.rtf
*.rtf
SCHEDULED_RUN_REPORT_*.md
